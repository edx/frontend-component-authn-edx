# Comment next release

name: Comment release expected version 

on:
  push:
    branches:
    - master
  pull_request:

jobs:
  version-check:
    uses: huniafatima-arbi/.github/.github/workflows/expected-release-version@huniafatima/release-expected-version-workflow